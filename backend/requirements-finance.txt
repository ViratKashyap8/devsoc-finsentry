# Finance Intelligence & RAG - install with: pip install -r requirements-finance.txt
# Core (already in pyproject.toml if using uv):
sentence-transformers>=2.2.0
transformers>=4.36.0
torch>=2.0.0
accelerate>=0.25.0
faiss-cpu>=1.7.4
numpy>=1.24.0
datasets>=2.14.0
# LoRA fine-tuning:
peft>=0.7.0
trl>=0.7.0
